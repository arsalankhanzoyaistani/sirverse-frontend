# netlify.toml — Fix React Router 404 refresh issue
[build]
  command = "npm run build"
  publish = "build"

# ✅ Redirect all routes to index.html (so React Router handles them)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
